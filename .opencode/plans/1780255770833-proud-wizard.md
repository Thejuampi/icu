# Documentation Update + Training Analysis

## Phase 1: Refresh intervals-icu skill

The user-provided canonical version matches the existing body content. Verify both `.agents/skills/intervals-icu/SKILL.md` and `.claude/skills/intervals-icu/SKILL.md` are in sync with the canonical text, updating only if diverged.

- **Files**: `.agents/skills/intervals-icu/SKILL.md`, `.claude/skills/intervals-icu/SKILL.md`

## Phase 2: Streamline cycling-performance-analysis skill

Rewrite to match the concise pattern of the intervals-icu skill:
- Operating Rule → Operating Principles (bullet rules, concise)
- Drop verbose Lessons Learned — move key ones inline
- Keep Primary Workflow numbered steps
- Keep Metric Policy, Planning Policy
- Apply same format to both `.agents/` and `.claude/` copies

- **Files**: `.agents/skills/cycling-performance-analysis/SKILL.md`, `.claude/skills/cycling-performance-analysis/SKILL.md`

## Phase 3: Update user-facing docs

### 3a. `docs/cli-reference.md`
- Add `--desc` format rules in events section (multi-line repeat blocks, no inline `NxMm`)
- Add `--upsert` behavioral explanation (upsertOnUid query param, uses external-id)
- Add `workout_doc` read-only note

### 3b. `docs/library.md`
- Document EventEx snake_case design decision (nolint:tagliatelle)
- Document workout_doc as read-only / generated server-side
- Explain camelCase (response DTOs like Event, Workout) vs snake_case (request DTOs like EventEx)

### 3c. `README.md`
- Add `events create` quick example with `--desc` in Quick Start

### 3d. `docs/analysis.md`
- Verify line 144 "Adaptation: nil" claim — update if code has changed

- **Files**: `docs/cli-reference.md`, `docs/library.md`, `README.md`, `docs/analysis.md`

## Phase 4: Analyze today's training (June 2, 2026)

```bash
icu activities list --oldest 2026-06-02 --newest 2026-06-02 --athlete-id i445643
icu wellness get 2026-06-02 --athlete-id i445643
```

Render: what was done, vs what was planned, TSS/IF/duration, wellness signals, recovery assessment for Thursday/Saturday decisions.

## Verification

```bash
go build ./... && go test ./... -count=1 && go vet ./... && golangci-lint run ./...
```

Verify:
- `icu help` shows events create with `--upsert` in help text
- `icu events create --help` shows usage including `[--upsert]`
- All skill files have correct frontmatter for their respective tools
- No outdated CLI command examples in docs (cross-check against `icu <resource> --help`)
